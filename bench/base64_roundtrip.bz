import "std"


fn main(): void {
    let sb = sb_new()
    let i = 0
    while (i < 20000) {
        sb = sb_append(sb, "abcdefghijklmnopqrstuvwxyz0123456789")
        i = (i + 1)
    }
    let s = sb_string(sb)
    let rounds = 20
    let j = 0
    let start = time_unix_millis()
    while (j < rounds) {
        let enc = base64_encode(s)
        let dec = base64_decode(enc)
        if !dec.is_ok {
            println(dec.err.message)
            return
        }
        if dec.value != s {
            println("mismatch")
            return
        }
        j = (j + 1)
    }
    let end = time_unix_millis()
    println(str((end - start)))
}

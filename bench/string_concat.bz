import "std"


fn main(): void {
    let n = 20000
    let s = ""
    let i = 0
    let start = time_unix_millis()
    while (i < n) {
        s = (s + "a")
        i = (i + 1)
    }
    let end = time_unix_millis()
    println(str((end - start)))
}
